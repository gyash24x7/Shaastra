import Button from "@shaastra/ui/button";

export default function LoginForm() {
	return (
		<form>
			<Button appearance = { "primary" } type = { "submit" } buttonText = { "Submit" } />
		</form>
	);
}