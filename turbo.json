{
	"$schema": "https://turbo.build/schema.json",
	"pipeline": {
		"dev": {
			"dependsOn": [ "^build", "db:generate", "gql:generate" ],
			"cache": false
		},
		"build": {
			"inputs": [ "../../../.swcrc", "src/**" ],
			"dependsOn": [ "^build", "db:generate", "gql:generate" ],
			"outputs": [ "dist/**" ]
		},
		"typecheck": {
			"dependsOn": [ "^typecheck", "gql:generate", "db:generate" ],
			"outputs": [ ]
		},
		"start": {
			"dependsOn": [ "build", "typecheck" ],
			"cache": false
		},
		"test": {
			"outputs": [ "coverage/**" ]
		},
		"db:push": {
			"inputs": [ "prisma/schema.prisma" ],
			"outputs": [ ]
		},
		"db:generate": {
			"inputs": [ "prisma/schema.prisma" ],
			"outputs": [ ]
		},
		"gql:generate": {
			"inputs": [ "src/assets/schema.graphql" ],
			"dependsOn": [ "schema:fetch" ],
			"outputs": [ "src/generated/**" ]
		},
		"schema:publish": {
			"inputs": [ "src/assets/schema.graphql" ],
			"outputs": [ ],
			"env": [
				"APOLLO_KEY",
				"APOLLO_GRAPH_REF"
			]
		},
		"schema:fetch": {
			"inputs": [ ],
			"dependsOn": [ "@shaastra/workforce#schema:publish", "@shaastra/connect#schema:publish" ],
			"outputs": [ "src/assets/schema.graphql" ],
			"env": [
				"APOLLO_KEY",
				"APOLLO_GRAPH_REF"
			]
		}
	}
}
