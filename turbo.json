{
	"$schema": "https://turbo.build/schema.json",
	"pipeline": {
		"dev": {
			"dependsOn": [ "db:generate", "gql:generate" ],
			"cache": false
		},
		"build": {
			"dependsOn": [ "^build", "db:generate", "gql:generate" ],
			"outputs": [ "dist/**" ]
		},
		"typecheck": {
			"dependsOn": [ "^typecheck", "gql:generate", "db:generate" ],
			"outputs": [ ]
		},
		"start": {
			"dependsOn": [ "build", "typecheck" ],
			"cache": false
		},
		"test": {
			"dependsOn": [ "typecheck" ],
			"outputs": [ "coverage/**" ]
		},
		"db:push": {
			"inputs": [ "prisma/schema.prisma" ],
			"outputs": [ ]
		},
		"db:generate": {
			"inputs": [ "prisma/schema.prisma" ],
			"outputs": [ ]
		},
		"gql:generate": {
			"inputs": [ "src/graphql/schema.graphql" ],
			"outputs": [ "src/graphql/generated/**" ]
		},
		"schema:publish": {
			"dependsOn": [ "src/graphql/schema.graphql" ],
			"outputs": [ ]
		}
	}
}
